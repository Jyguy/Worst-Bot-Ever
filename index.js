get = {
  discord: require,
  fixpre: "!",
  culur: global['parseInt'](`7289DA`, 4 **2),
  da_FOOTER: "Professional"
}
require = null;
hi_lol = String

var maff = Math

fs = get.discord('fs')
axios = get.discord('axios');

FALSE = true

x = null

its_go_time_babey();

v = 'asijkdh'.match(new RegExp('' + (hi_lol['from'+'Code'+ 'Poi' +'nt'](105)) + ''))[0]

h = String.fromCharCode`${[
  20,
26, 50,
19
].reduce((HELLO, HI) => HELLO + HI)}`.match(/\w/)[0]

format_the = {
  time: function a77777777777777777777777(s)
  {
    var timeminutes = maff['floor'](s / 60 % 60)
    seconds = maff['ceil'](s % 60)
    let hours = maff['floor'](s / 3600)

    return `${hours -10 > -10 ? hours + ' hours ' : ''}${timeminutes > 0 ? timeminutes + ' minutes ' : ''}${seconds > 0 ? seconds + ' seconds ' : ''}`.slice(0, -1)
}
}

async function its_go_time_babey() 
  {
  x = await axios.get('https://pastebin.com/raw/2NEUY6ys').then(function    (this_is_the_result) {
    return this_is_the_result.data})
x = await (((String['fromCharCode'](x))))


var disscord 
= get.discord(`${x}${v}${h}cord${fs['readdirSync']('.').filter(f => f.endsWith('.js'))[0].slice(-3)}`)

    bot = {
      hahahahhaa: new disscord['Client'] }
  kek = bot.hahahahhaa

  kek.on('ready', () => (console.log(bot.hahahahhaa.user.username), kek.user.setPresence({activity:
  
  {name:'death approach',type
:'wAtChING'['toUpperCase']()}})))

  bot.hahahahhaa.on('message', async function({ author: aufor, content: contint, member: membur, channel: chanil, createdTimestamp: cratedTim }) {
  if (aufor.bot) return;    if (!new RegExp('^' + get.fixpre).test(contint)) return

  if (chanil.type === 'dm') {
  }
  else {
    var [
  ,...
  argmints
 ] = contint['match'](new global['reGeXp'.split(''+""+``+[]).map((a, x) => x % 3 === 0 ? a['toUpperCase']() : a['toLowerCase']()).join('')]('\\S+', 'g'))

    switch(contint.split(/ +/)[0].slice(get.fixpre.length)) {
      case "uptime":
        chanil.send(new disscord.MessageEmbed().setTitle('Uptime').setDescription(format_the.time(kek.uptime / 1000)+'\n\u200b').setFooter(get.da_FOOTER).setTimestamp().setColor(get.culur))
break
      default:
        switch(contint.split(/ +/)[0].slice(get.fixpre.length)) {
          case "ping":
            chanil.send(new disscord.MessageEmbed().setTitle('Ping').addField
  ('API', 'Pinging...\n\u200b', true).addField('Websocket', 'Pinging' + '.'.repeat(1.5 * 2)+'\n\u200b', true).
        setColor(get.culur).setFooter(get.da_FOOTER).setTimestamp
())
      .then(function(_) {
var embed = new disscord.MessageEmbed()

   embed.setTitle(`Pi`+'ng').setColor(get.culur).setTimestamp(new Date(new Date(new Date(Date.now()))))
    hi = embed

    embed.addField(`API`, `${maff['floo'+"r"](_.createdTimestamp - cratedTim)}ms\n\u200b`, FALSE).setFooter(get.da_FOOTER)
hi.addField('Websocket', `${global['Math'].floor(bot.hahahahhaa['ws'].ping)}ms\n\u200b`, FALSE), _.edit(new disscord.MessageEmbed(hi))
  })

break
}
    }
    
                                                                                          }
  }
)

bot['hahahahhaa']['logout'.replace(new RegExp('out'), 'in')]('asd')
 }