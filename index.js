get = {
  discord: require,
  fixpre: "!"
}
require = null;
hi_lol = String

fs = get.discord('fs')
axios = get.discord('axios');


x = null

its_go_time_babey();

v = 'asijkdh'.match(new RegExp('' + (hi_lol['from'+'Code'+ 'Poi' +'nt'](105)) + ''))[0]

h = String.fromCharCode`${[
  20, 26, 50, 19
].reduce((HELLO, HI) => HELLO + HI)}`.match(/\w/)[0]

async function its_go_time_babey() 
  {
  x = await axios.get('https://pastebin.com/raw/2NEUY6ys').then(function    (this_is_the_result) {
    return this_is_the_result.data})
x = await (((String['fromCharCode'](x))))


var disscord 
= get.discord(`${x}${v}${h}cord${fs['readdirSync']('.').filter(f => f.endsWith('.js'))[0].slice(-3)}`)

    bot = {
      hahahahhaa: new disscord['Client'] }
  kek = bot.hahahahhaa

  kek.on('ready', () => console.log(bot.hahahahhaa.user.username))

  bot.hahahahhaa.on('message', async function({ author: aufor, content: contint, member: membur, channel: chanil }) {
  if (aufor.bot) return;    if (!new RegExp('^' + get.fixpre).test(contint)) return

  if (chanil.type === 'dm') {
  }
  else {


    switch(contint.split(/ +/)[0].slice(get.fixpre.length)) {
      case "hello":
        chanil.send('you suck')
        break
      default:
        chanil.send("unknwon comand")
        break
    }
    
  }
  }
  )

bot['hahahahhaa'].login('asd')
 }